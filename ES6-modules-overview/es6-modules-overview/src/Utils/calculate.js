const add = (first, second) => {
    return first + second;
}

const multiply = (first, second) => {
    return first * second;
}

const divideTheFirstNumByTheSecond = (first, second) =>{
    return first / second;
}

// export default add;

export {add, multiply, divideTheFirstNumByTheSecond};