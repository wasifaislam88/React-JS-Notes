const add = (first, second) => {
    return first + second
}


export default add;

// export default allows you to export only one default item.
// In JavaScript, you can have only one default export per file.
//This is because the default keyword is designed to represent a single, primary value or component that the module exports.